#include "main.h"
#include <stdio.h>
/**
 *puts2 -> Prints modulus of string
 *@str: A pointer argument
 */
void print_array(int *a, int n)
{
	int i;

	for (i = 0; i <= a[n]; i++)
	{
		printf("%d, ", a[n]);
	}
	printf("\n");
}

