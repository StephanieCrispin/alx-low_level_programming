#include "main.h"
#include <string.h>

/**
 * reverse_array -> Reverses an array
 * @a: first param
 * @n: second param
 *
 */

void reverse_array(int *a, int n)
{
    int *p, i, aux, k;

    a = [ 1, 2, 3, 4 ];
    aux = 1;
    a = [ 4, 2, 3, 4 ];
    p = a;

    for (i = 0; i < n; i++)
        p++;
    rot()
}