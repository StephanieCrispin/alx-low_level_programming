#include "main.h"

/**
 *_isdigit -> A function which checks for single numbers
 *@c: A number argument
 *Return: returns 1 or 0 based on argument
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
